## 2024-05-23 - Accessibility Improvements
**Learning:** Adding visual cues like `*` and `required` attributes to inputs is good, but without updating the JavaScript validation logic, it creates a mismatch between user expectation and system behavior.
**Action:** Always verify and update the corresponding JS validation logic when modifying HTML form constraints.
