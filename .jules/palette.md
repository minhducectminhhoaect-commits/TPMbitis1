## 2026-01-10 - Persistent Labels for Forms
**Learning:** The application relied on placeholders for form inputs, which disappear when typing and are less accessible.
**Action:** Always ensure input fields have associated `<label>` elements with matching `for` attributes, using the `.input-label` class for consistency.
