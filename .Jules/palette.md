# Palette's Journal

## 2024-05-22 - Replacing Native Alerts with Toasts
**Learning:** Native `alert()` calls are blocking and disrupt the user's workflow, creating a jarring experience. In single-page apps (SPAs), maintaining context is crucial.
**Action:** Replace all blocking `alert()` calls with non-blocking, transient toast notifications to provide smooth feedback without interruption.
